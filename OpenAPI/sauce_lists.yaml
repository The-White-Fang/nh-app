openapi: 3.0.0
info:
  title: Sauce Lists API
  description: Custom sauce lists CRUD and pinning API. Scoped to sauce (n_titles) content only.
  version: 1.0.0
servers:
  - url: /

paths:
  /sauce/lists:
    post:
      summary: Create a new sauce list
      tags:
        - Sauce Lists
      security:
        - BearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - name
              properties:
                name:
                  type: string
                  maxLength: 255
                description:
                  type: string
                  maxLength: 1024
                  nullable: true
      responses:
        '201':
          description: List created successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListOverview'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/InternalServerError'

    get:
      summary: Fetch all sauce lists for the authenticated user
      tags:
        - Sauce Lists
      security:
        - BearerAuth: []
      responses:
        '200':
          description: All sauce lists for the current user
          content:
            application/json:
              schema:
                type: object
                properties:
                  lists:
                    type: array
                    items:
                      $ref: '#/components/schemas/ListOverview'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/InternalServerError'

  /sauce/lists/{id}:
    get:
      summary: Fetch a single sauce list with its items
      tags:
        - Sauce Lists
      security:
        - BearerAuth: []
      parameters:
        - $ref: '#/components/parameters/ListId'
      responses:
        '200':
          description: List detail with sauce items
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListDetail'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalServerError'

    patch:
      summary: Update sauce list name or description
      tags:
        - Sauce Lists
      security:
        - BearerAuth: []
      parameters:
        - $ref: '#/components/parameters/ListId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                  maxLength: 255
                description:
                  type: string
                  maxLength: 1024
                  nullable: true
      responses:
        '200':
          description: List updated successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListOverview'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalServerError'

    delete:
      summary: Delete a sauce list and all its items
      tags:
        - Sauce Lists
      security:
        - BearerAuth: []
      parameters:
        - $ref: '#/components/parameters/ListId'
      responses:
        '204':
          description: List deleted successfully
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalServerError'

  /sauce/lists/{id}/pin:
    patch:
      summary: Toggle pin/unpin a sauce list
      tags:
        - Sauce Lists
      security:
        - BearerAuth: []
      parameters:
        - $ref: '#/components/parameters/ListId'
      responses:
        '200':
          description: Pin status toggled
          content:
            application/json:
              schema:
                type: object
                properties:
                  is_pinned:
                    type: boolean
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalServerError'

  /sauce/lists/{id}/items:
    post:
      summary: Add a sauce item to a list
      tags:
        - Sauce Lists
      security:
        - BearerAuth: []
      parameters:
        - $ref: '#/components/parameters/ListId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - sauce_id
              properties:
                sauce_id:
                  type: integer
      responses:
        '201':
          description: Sauce added to list
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListItem'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          description: Sauce already exists in the list
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
              example:
                error: DUPLICATE_ITEM
                message: This sauce is already in the list
        '500':
          $ref: '#/components/responses/InternalServerError'

  /sauce/lists/{id}/items/{itemId}:
    delete:
      summary: Remove a sauce item from a list
      tags:
        - Sauce Lists
      security:
        - BearerAuth: []
      parameters:
        - $ref: '#/components/parameters/ListId'
        - in: path
          name: itemId
          required: true
          schema:
            type: integer
      responses:
        '204':
          description: Sauce removed from list
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalServerError'

components:
  securitySchemes:
    BearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT

  parameters:
    ListId:
      in: path
      name: id
      required: true
      schema:
        type: integer
      description: The sauce list ID

  schemas:
    ListOverview:
      type: object
      properties:
        id:
          type: integer
        name:
          type: string
        description:
          type: string
          nullable: true
        is_pinned:
          type: boolean
        is_default:
          type: boolean
        item_count:
          type: integer
          description: Total number of sauce items in the list
        preview_images:
          type: array
          items:
            type: string
          description: Cover images of the first 4 sauce items
        created_at:
          type: string
          format: date-time

    ListDetail:
      type: object
      properties:
        id:
          type: integer
        name:
          type: string
        description:
          type: string
          nullable: true
        is_pinned:
          type: boolean
        is_default:
          type: boolean
        item_count:
          type: integer
        created_at:
          type: string
          format: date-time
        items:
          type: array
          items:
            $ref: '#/components/schemas/ListItem'

    ListItem:
      type: object
      properties:
        id:
          type: integer
        sauce_id:
          type: integer
        title:
          type: string
        image_url:
          type: string
          nullable: true
        added_at:
          type: string
          format: date-time

    ErrorResponse:
      type: object
      properties:
        error:
          type: string
        message:
          type: array
          items:
            type: string

  responses:
    BadRequest:
      description: Bad Request
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'
    Unauthorized:
      description: Unauthorized - Missing or invalid JWT
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'
    NotFound:
      description: Resource not found
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'
    InternalServerError:
      description: Internal Server Error
